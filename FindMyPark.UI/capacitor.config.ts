import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.findmypark.app',
  appName: 'FindMyPark',
  webDir: 'dist/find-my-park.ui/browser'
};

export default config;
